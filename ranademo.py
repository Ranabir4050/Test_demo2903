
    Essential Commands for "vi" editor:

## **1. Navigation Commands**
- `h` → Move left
- `l` → Move right
- `j` → Move down
- `k` → Move up
- `Ctrl + d` → Move half-page down
- `Ctrl + u` → Move half-page up
- `Ctrl + f` → Move full-page forward
- `Ctrl + b` → Move full-page backward
- `G` → Move to the last line
- `gg` → Move to the first line

### **2. Editing Commands**
- `i` → Insert before cursor
- `a` → Append after cursor
- `o` → Open a new line below
- `O` → Open a new line above
- `x` → Delete character at cursor
- `dd` → Delete current line
- `yy` → Copy (yank) current line
- `p` → Paste below cursor
- `P` → Paste above cursor
- `u` → Undo last change
- `Ctrl + r` → Redo last undone change

### **3. Search & Replace**
- `/text` → Search forward for "text"
- `?text` → Search backward for "text"
- `n` → Repeat last search forward
- `N` → Repeat last search backward
- `:%s/old/new/g` → Replace all occurrences of "old" with "new"

### **4. Saving & Exiting**
- `:w` → Save file
- `:q` → Quit vi
- `:wq` → Save and quit
- `:q!` → Quit without saving

### **5. Advanced Commands**
- `:set number` → Show line numbers
- `:set ignorecase` → Ignore case in searches
- `:r filename` → Read and insert content from another file
- `:!command` → Run a shell command without exiting vi

Would you like a cheat sheet or a practice guide to help you master these commands? 😊
